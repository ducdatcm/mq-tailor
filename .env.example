# Copy this file to .env on the server (or locally) and fill in real values.
# Never commit the real .env file.

# --- App ---
NODE_ENV=production
PORT=3000
BASE_URL=https://your-domain.com

# --- Session ---
# Any long random string. Generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=change-me-to-a-long-random-string

# --- MySQL (create this database + user in Hostinger hPanel first) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# --- Cloudflare R2 (required — uploaded photos are stored here, not on
# local disk, since this host gives every deploy a fresh disposable folder).
# Create a bucket + API token at dash.cloudflare.com -> Storage & Databases
# -> R2 Object Storage. Enable "Public Development URL" on the bucket for
# R2_PUBLIC_URL below.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=https://pub-xxxxxxxxxxxx.r2.dev

# --- One-time admin seed (run `npm run seed:admin`, then remove these two lines) ---
ADMIN_SEED_USERNAME=admin
ADMIN_SEED_PASSWORD=change-me-before-seeding

# --- Outgoing mail for enquiry notifications (optional — leave blank to skip email relay) ---
# Example for a Gmail App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Minh Quang Tailoring House" <no-reply@your-domain.com>
ENQUIRY_NOTIFY_TO=minhquanghotline@gmail.com

# --- Analytics (optional — leave blank to disable) ---
GA_MEASUREMENT_ID=

# --- Keep a real domain dark while testing on a temporary URL (optional) ---
# Comma-separated hostnames to block with a "not available yet" page.
# Leave blank once you're ready to go public on that domain.
PRIVATE_DOMAINS=
