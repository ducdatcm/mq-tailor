<section class="section container container--narrow text-center">
  <span class="eyebrow">404</span>
  <h1><%= t('not_found.heading') %></h1>
  <p class="lede" style="margin-inline:auto;"><%= t('not_found.body') %></p>
  <a class="btn" href="/<%= lang %>/"><%= t('not_found.back_home') %></a>
</section>
